#-----------------------------------------------------------------------------------------------------------------------------
#---PROJECT PARAMETERS---

Out_dir_path = /home/urbe/Tools/TROUVER/sampleDATA/

project_dir_path =/home/urbe/Tools/TROUVER/pipeline/

#-----------------------------------------------------------------------------------------------------------------------------
#---GENERAL SETTINGS

mode = del	                        # main analysis mode. Currently ALIEN supports only gene and random models analysis.
max_processors = 20			# Number of processor for BLAST only

#remove_identical = yes                 # "yes" to remove, "no" otherwise
verbose = 1      			#Print all detail #1 to print nice log messages telling you what is going on. 0 otherwise

#-----------------------------------------------------------------------------------------------------------------------------

R1=/home/urbe/DATA/MarcDATA/4Jitendra_bigdeletions/del_toy3reads35_1.fq
R2=/home/urbe/DATA/MarcDATA/4Jitendra_bigdeletions/del_toy3reads35_2.fq

#-----------------------------------------------------------------------------------------------------------------------------
#---SEQUENCE/GROUP PARAMETERS---

behavior_about_bad_clusters = 1          # what should tool do if it finds a cluster with a sequence removed
                                         # due to any filter? Possible options are:
                                         # 0 - does not filter any sequence (not recommended)
                                         # 1 - removal of any flagged sequence 
                                         # 2 - removal of any group with flagged sequences

validation_criteria = all                # quality criteria to remove sequences. Possible values are:
                                         # 1 - checks for valid start codons
                                         # 2 - checks for valid stop codons
                                         # 3 - checks for sequence size multiple of 3
                                         # 4 - checks for nucleotides outside ATCG
                                         # 'all' applies every verification

#additional_start_codons = ()             # these codons, plus the ones specified in codon table, will be the valid start
                                         # codons for validation purposes
#additional_stop_codons = ()              # same as start codons

codon_table = 11

absolute_min_sequence_size = 500         # minimum sequence length cutoff for sequence/group further evaluation !!!

absolute_max_sequence_size = 10000       # maximum sequence length cutoff for sequence/group further evaluation !!!!!

min_group_identity = 70                 # mean minimum group identity cutoff in pairwise sequence alignments

max_group_identity = 100                # mean maximum group identity cutoff in pairwise sequence alignments

group_identity_comparison = gene          # the kind of sequence that will be used when computing aliens !!!!!!

min_sequence_identity = 70               # minimum (mean/median) sequence identity cutoff in pairwise sequence alignments

max_sequence_identity = 100             # maximum (mean/median) sequence identity cutoff in pairwise sequence alignemnts

sequence_identity_comparison = aa       # the kind of sequence that will be used when computing sequence identity
                                        # possible values are "nt" and "aa"
#--------------------------------------------------------------------------------------------------------------------------
#BASE FILES

reference_genome_file =  /home/urbe/DATA/freebayes/bwa/vagaN.fa   # genome reference name, leave blank for none (same name used in fasta file)

bamfile = /home/urbe/DATA/freebayes/bwa/control.sorted.bam

#--------------------------------------------------------------------------------------------------------------------------
#---THIRD-PARTY SOFTWARE CONFIGURATION---

multiple_alignment = bwa                # program used for multiple sequence alignment. Possible values are   !!!!
                                        # bowtie, bwa and segemehal

species_name_augustus = caenorhabditis  # Name of the species for augustus gene predictions !!!

phylogenetic_tree = bwa                 # program used for phylogenetic tree reconstruction. Possible values are
                                        # proml dnaml, phyml_aa and phyml_nt

blastopt = offline			 # Blast running mode: Possible values are "online" or "offline" !!!!
evalue = 1e-5				 # Blast evalue setting !!!!

pvalue = 0.05                            # p-values for positive selection detection
qvalue = 0.05                            # q-values for positive selection detection
